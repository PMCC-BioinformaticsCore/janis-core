
nextflow.enable.dsl=2
docker.enabled = true

params {
    
    // Macro to all optional task inputs 
    // (do not alter unless you know what you are doing)
    NULL = 'NULL'

    // WORKFLOW OUTPUT DIRECTORY
    outdir  = './outputs'

    // INPUTS (MANDATORY)
    in_forward_reads  = NULL  // (MANDATORY generic file)  
    in_long_reads     = NULL  // (MANDATORY generic file)  
    in_reverse_reads  = NULL  // (MANDATORY generic file)  


}
